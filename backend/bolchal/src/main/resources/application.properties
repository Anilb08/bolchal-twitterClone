# Spring Application Name
spring.application.name=bolchal

# DataSource Configuration for MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/bolchal_main?useSSL=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Bulbul@7620

# Hibernate Dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate DDL Auto - Update to automatically update the schema
spring.jpa.hibernate.ddl-auto=update

# JPA Show SQL (Optional, helpful for debugging)
spring.jpa.show-sql=true

# Server Port
server.port=8080
